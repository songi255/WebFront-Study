1. npm run build 로 dist 폴더 생성
2. aws 서버에 옮기기
3. nginx 설정
    - 설치 : sudo apt update
            sudo apt install nginx
    - 설정
         - sudo nano /etc/nginx/sites-available/default
            수정
                - root /home/ubuntu/dist;
                - location에 try_files $uri $uri/ /index.html; 로 수정
    - 적용(재시작)
        - sudo service nginx reload